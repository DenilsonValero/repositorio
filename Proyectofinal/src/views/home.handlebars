<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Productos</title>
</head>
<body>
    {{{body}}}
    <h1>Lista de Productos</h1>
    <ul>
        {{#each products}}
            <li>{{this.name}} - ${{this.price}}</li>
        {{/each}}
    </ul>
</body>
</html>